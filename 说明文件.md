以下是整理优化后的 Markdown 说明文档，包括格式美化和用户输入部分的说明清晰化：

---

# 使用说明文档（WSL 环境）

## 1. 环境配置

### 创建并激活 Conda 环境
```bash
conda create -yn gspl python=3.9 pip
conda activate gspl
```

## 2. 安装依赖

### 安装 PyTorch 及 CUDA 对应版本
```bash
pip install -r requirements/pyt201_cu118.txt
```

### 安装 CUDA Toolkit
```bash
conda install -c "nvidia/label/cuda-11.8.0" cuda-toolkit
```

### 安装其他依赖项
```bash
pip install ninja
pip install -r requirements.txt
```

### 安装 FFmpeg 和 COLMAP
```bash
conda install -c conda-forge colmap
sudo apt update
sudo apt install ffmpeg
```

### 安装对齐功能所需依赖
```bash
pip install cupy-cuda11x
pip install probreg
```

---

## 3. 数据预处理与位姿估计

```bash
python preprocessing/process_images.py \
--input_folder <输入视频文件夹路径> \
--output_folder <输出文件夹路径> \
--total_frames <提取帧数，例如 300> \
--refine_intrinsics
```

> 建议帧数为 300 左右，可处理多个视频。

处理完成后请注意查看输出信息：
```
[Info] 使用图像数最多的模型： <输出路径>（图像数：295）
```

如果图像数显著少于提取帧数（如 <150），建议：
- 增加提取帧数
- 或重新拍摄视频以提升图像匹配效果

**⚠️ 注意**：未来将增加参数以优化图像匹配宽容度。

---

## 4. 开始场景重建

```bash
python main.py fit \
--config <重建配置文件路径> \
--data.path <数据集路径（即 output_folder/undistorted）> \
--data.parser.init_args.down_sample_factor <下采样倍数，例如 2> \
-n <场景名称> \
-v <版本标识> \
--viewer
```


### 🔽 下采样倍数说明（`--data.parser.init_args.down_sample_factor`）

- 该参数用于 **降低图像分辨率**，以提升重建与分割速度并降低显存占用。
- 推荐设置：  
  - 若 **显存小于 16GB**, 原始图像宽或高 ÷ 下采样倍数 ≲ **1000**；
  - 例如对 1080p 视频，建议使用 `2` 倍下采样。


### 配置文件选择建议：

| 场景情况 | 建议配置文件 |
|----------|-----------------------------|
| 拍摄质量较高，无移动物体 | `configs/my_configs/simple_config.yaml` |
| 有曝光不均或移动物体 | `configs/my_configs/appearance_config.yaml` |
| 拍摄覆盖不足，重建效果差 | 深度估计 + `appearance_depth_config.yaml` |

---

## 5. 深度估计（如需）

```bash
python utils/estimate_dataset_depths.py <数据集路径> \
--image_dir images_<下采样倍数>
```

> 例：若使用 2 倍下采样，请使用 `--image_dir images_2`

完成后使用配置：
```yaml
configs/my_configs/appearance_depth_config.yaml
```

确保与深度估计时的下采样倍数一致！

---

## 6. 查看重建过程 / 结果

### 重建过程中查看（开启 --viewer）
- 控制台中将显示 URL，Ctrl+点击或手动打开：http://localhost:8080

### 重建完成后查看：
```bash
python viewer.py outputs/<重建场景>/<重建版本>
```

---

## 7. 开始场景分割

### 自动提取二维掩码
```bash
python utils/get_sam_masks.py \
<数据集路径>/images_<下采样倍数>
```

> 耗时依赖于显卡性能和图像尺寸，可能需 30 分钟~数小时。

### 将二维掩码转换为三维分割
```bash
python seganygs.py fit \
--config configs/segany_splatting.yaml \
--data.path <数据集路径> \
--data.parser.down_sample_factor <下采样倍数> \
--model.initialize_from outputs/<重建场景>/<重建版本> \
-n <重建场景> -v <分割版本>
```

### 查看分割结果
```bash
python viewer.py \
outputs/<重建场景>/<分割版本>
```

---

## 8. 场景元素操作（可视化界面）

此处详见说明视频

### 场景元素选取：
> 提供交互式图形界面（未具体说明）

### 对齐已知模型：
> 提供交互式图形界面（未具体说明）

### 保存场景元素：
> 提供交互式图形界面（未具体说明）

---

## 9. 转换模型为简化可视化模型

```bash
python postprocess.py \
--load_model outputs/<重建场景>/<分割版本> \
--output_dir <输出路径>/<文件名>
```

